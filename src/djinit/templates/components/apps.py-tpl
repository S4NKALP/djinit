from django.apps import AppConfig



class [[ app_config_name ]]Config(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
# @IF app_module
    name = '[[ app_module ]]'
# @ENDIF
# @IF not app_module
    name = '[[ app_name ]]'
# @ENDIF
